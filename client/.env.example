# Local Vite: point at the API directly.
# Docker (nginx proxy): use /api/v1 so the browser stays same-origin.
VITE_API_BASE_URL=http://127.0.0.1:4000/api/v1
VITE_API_TIMEOUT_MS=10000
VITE_DOCUMENT_API_TIMEOUT_MS=120000
